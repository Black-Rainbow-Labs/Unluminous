
# Agent Chat
The settings aren't scrollable.  
![img_2.png](img_2.png)

I'm also not seeing any errors when I try to chat with my agent.  Nothing happens.  The send button turns into a stop button,then back.
my message isn't shown, and no error.  figure out the error, fix it, and also ensure errors are displayed as a well designed toast message that can be dismissed.

Unluminous tools should be enabled by default.

Get rid of the file picker icon.  just allow drag and drop or paste.

# General 


Plugins menu items at the top should be moved to a Plugins menu item, which lists each plugin, and has sub menus for their options.

Database tab at the bottom left of the left side menu items doesn't seem to do anything. We have a db tab that does work to show/hide. 
Agent tasks tab is weird too.  I can't untoggle it to hide it.  it should always open/close

Resize icons are weird.  eg theres one on the left edge that points left with a bar, that doesn't do anything when I drag and click once its shown.
The two arrow icon shows, and that allows me to resize.  get rid of the one arrow with bar options on the edges.

Agent tasks should be it's own pane, rather than a tab.   
Database query view should be part of the database pane, rather than a separate tab.

# Markdown
Links in markdown should allow me to CMD/Ctrl+Click to open them in a new browser window.

# Github branch
Add a branch selector/indicator at the top bar, similar to Intellij's
![img_3.png](img_3.png)